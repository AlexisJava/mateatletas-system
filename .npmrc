# NPM Configuration for Mateatletas Monorepo

# Enable workspaces
enable-pre-post-scripts=true

# Legacy peer deps (necesario para algunas dependencias)
legacy-peer-deps=true

# Auto install peers
auto-install-peers=true

# Preferir workspace packages sobre npm registry
prefer-workspace-packages=true
