# NPM Configuration for Mateatletas Monorepo

# Legacy peer deps (necesario para algunas dependencias)
legacy-peer-deps=true

# Engine strict (permitir instalar con versiones de node ligeramente diferentes)
engine-strict=false
