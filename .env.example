# ============================================================
# MATEATLETAS - VARIABLES DE ENTORNO
# ============================================================
# Copia este archivo a .env y completa los valores
# NUNCA commitees el archivo .env con valores reales
# ============================================================

# ============ DATABASE ============
# URL de conexión a PostgreSQL (required)
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://mateatletas:password@localhost:5432/mateatletas

# ============ AUTH / JWT ============
# Secreto para firmar tokens JWT (required)
# Generar con: openssl rand -base64 32
JWT_SECRET=genera-con-openssl-rand-base64-32-minimo-32-caracteres

# Tiempo de expiración del JWT (optional, default: 1h)
JWT_EXPIRATION=1h

# Rounds de bcrypt para hashing de passwords (optional, default: 12)
BCRYPT_ROUNDS=12

# ============ MERCADOPAGO ============
# Token de acceso de MercadoPago (required para pagos reales)
# Obtener en: https://www.mercadopago.com.ar/developers/panel/app
# Si no se configura, el sistema usa modo mock
MERCADOPAGO_ACCESS_TOKEN=APP_USR-tu-access-token-de-mercadopago

# Secreto para validar webhooks de MercadoPago (required para webhooks)
# Obtener en la configuración de webhooks de MercadoPago
MERCADOPAGO_WEBHOOK_SECRET=tu-webhook-secret-de-mercadopago

# IPs permitidas para webhooks (optional, override de emergencia)
# Formato: lista separada por comas
# MERCADOPAGO_ALLOWED_IPS=216.33.196.0/24,52.29.176.0/24

# ============ REDIS ============
# URL de conexión a Redis (optional, requerido para cache/queues)
# Formato: redis://[:password@]host:port[/database]
REDIS_URL=redis://localhost:6379

# ============ APP CONFIG ============
# Entorno de ejecución (required)
NODE_ENV=development

# Puerto del servidor (optional, default: 3001)
PORT=3001

# Host del servidor (optional, default: 0.0.0.0)
HOST=0.0.0.0

# URL del frontend para CORS y redirecciones (required)
FRONTEND_URL=http://localhost:3000

# URL del backend (optional, usado internamente)
BACKEND_URL=http://localhost:3001

# ============ RATE LIMITING ============
# Máximo de requests por ventana de tiempo (optional)
RATE_LIMIT_MAX=100

# Ventana de tiempo en segundos (optional)
RATE_LIMIT_TTL=60

# ============ ADMIN INICIAL ============
# Credenciales para crear admin inicial en seed (optional)
ADMIN_EMAIL=admin@mateatletas.com
ADMIN_PASSWORD=genera-password-seguro-minimo-12-caracteres

# ============ NOTIFICACIONES ============
# Webhook de Slack para alertas (optional)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx/yyy/zzz

# ============ VERCEL BLOB (Frontend) ============
# Token para Vercel Blob Storage (optional, solo si usa Vercel Blob)
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxx

# ============ TESTING ============
# URL base para Playwright (optional, solo en testing)
# PLAYWRIGHT_BASE_URL=http://localhost:3000

# Workers paralelos para Playwright (optional)
# PLAYWRIGHT_WORKERS=4

# ============================================================
# NOTAS DE SEGURIDAD:
# ============================================================
# 1. En producción, usa secretos fuertes generados aleatoriamente
# 2. JWT_SECRET debe tener minimo 32 caracteres
# 3. Nunca reutilices secretos entre ambientes
# 4. Usa variables de Railway/Vercel para produccion
# 5. MERCADOPAGO_ACCESS_TOKEN sin configurar activa modo mock
# ============================================================
