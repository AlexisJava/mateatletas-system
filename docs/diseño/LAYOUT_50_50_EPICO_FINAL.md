# 🎮 LAYOUT 50/50 ÉPICO - SIN SCROLL - VERSIÓN FINAL

## 🎯 REGLAS DE ORO

1. ✅ **LAYOUT 50/50 SIEMPRE** - Avatar en un lado, Info en el otro
2. ✅ **SIN SCROLL NUNCA** - Todo visible en viewport (100vh)
3. ✅ **AVATAR GIGANTE** - Domina su mitad de pantalla (50%)
4. ✅ **HEADER MINIMALISTA** - Solo recursos y nivel (5vh)
5. ✅ **BOTTOM BAR ÉPICO** - XP brillante + navegación (10vh)
6. ✅ **MENÚ MODAL BRAWL STARS** - Full-screen tipo videojuego

---

## 📱 MOBILE LANDSCAPE (480px - 667px)

```
┌─────────────────────────────────────────────────────────────┐
│  💰 1,250  🔥 7  [NIVEL 12]                                │ ← HEADER (5vh)
├──────────────────────────────┬──────────────────────────────┤
│                              │                              │
│                              │   NIVEL 12                   │
│        🧙‍♂️                    │   ┌──────────────┐          │
│                              │   │              │          │
│    AVATAR 3D GIGANTE         │   │  Badge nivel │          │
│    (Lado izquierdo)          │   │              │          │
│    50% de ancho              │   └──────────────┘          │ ← MAIN (85vh)
│                              │                              │   50/50 SPLIT
│   Resplandor neón            │   🏆 12  🔥 7   💎 85%      │   SIN SCROLL
│   Clickeable                 │   (Stats mini)              │
│                              │                              │
│                              │   📅 PRÓXIMA CLASE           │
│                              │   Hoy • 14:00                │
│                              │                              │
├──────────────────────────────┴──────────────────────────────┤
│ NIVEL 12 ████████████░░░░ 13  •  850/1000 XP  •  [☰ MENÚ] │ ← BOTTOM (10vh)
└─────────────────────────────────────────────────────────────┘
```

### Características Mobile:

- **Header:** 5vh compacto (solo recursos + nivel)
- **Main:** 85vh dividido 50/50
  - **Izquierda:** Avatar 3D gigante (50% ancho, 100% alto)
  - **Derecha:** Info vertical (nivel badge, stats mini, próxima clase)
- **Bottom:** 10vh (XP bar + botón MENÚ)

---

## 📱 TABLET LANDSCAPE (768px - 1024px)

```
┌───────────────────────────────────────────────────────────────────┐
│  👤 ALEX • Nivel 12         MATEATLETAS        💰 1,250  🔥 7    │ ← HEADER (5vh)
├─────────────────────────────┬─────────────────────────────────────┤
│                             │                                     │
│                             │         NIVEL 12                    │
│        🧙‍♂️                   │   ┌──────────────────────┐        │
│                             │   │    Badge grande      │        │
│    AVATAR 3D GIGANTE        │   └──────────────────────┘        │
│    (Lado izquierdo)         │                                     │
│    50% de ancho             │   PROGRESO AL NIVEL 13              │ ← MAIN (85vh)
│                             │   ██████████░░  850/1000 XP         │   50/50 SPLIT
│   Resplandor neón gigante   │                                     │   SIN SCROLL
│   Partículas mágicas        │   ┌──────┬──────┬──────┐          │
│   Clickeable para animar    │   │  🔥  │  🏆 │  🎯 │          │
│                             │   │ 7 d  │12/50│ 85% │          │
│                             │   └──────┴──────┴──────┘          │
│                             │                                     │
│                             │   📅 PRÓXIMA CLASE                  │
│                             │   Álgebra • HOY • 14:00             │
├─────────────────────────────┴─────────────────────────────────────┤
│ NIVEL 12 ████████████████░░░░ 13  •  850/1000  •  🏠 🧠 📖 ☰    │ ← BOTTOM (10vh)
└───────────────────────────────────────────────────────────────────┘
```

### Características Tablet:

- **Header:** 5vh con logo central
- **Main:** 85vh dividido 50/50
  - **Izquierda:** Avatar 3D gigante (50%, 100% alto)
  - **Derecha:** Info completa (badge, XP bar, stats 3 cols, próxima clase)
- **Bottom:** 10vh (XP bar + 4 botones navegación)

---

## 💻 DESKTOP (1280px+)

```
┌──┬────────────────────────────────────────────────────────────┬──┐
│🏠│ 👤 ALEX • Nivel 12       MATEATLETAS      💰 1,250  🔥 7  │🎨│ ← HEADER (5vh)
│  ├──────────────────────────┬─────────────────────────────────┤  │
│🧠│                          │                                 │👥│
│  │                          │        NIVEL 12                 │  │
│📖│        🧙‍♂️                │   ┌─────────────────────┐      │📊│
│  │                          │   │   Badge gigante     │      │  │
│🏆│   AVATAR 3D GIGANTE      │   └─────────────────────┘      │🔔│
│  │   (Lado izquierdo)       │                                 │  │ ← MAIN (85vh)
│🛒│   50% de ancho           │   PROGRESO AL NIVEL 13          │  │   50/50 SPLIT
│  │                          │   ████████████░░  850/1000 XP   │🚪│   SIN SCROLL
│  │  Resplandor neón épico   │                                 │  │
│  │  Partículas mágicas      │   ┌──────┬──────┬──────┐       │  │
│  │  Clickeable para animar  │   │  🔥  │  🏆 │  🎯 │       │  │
│  │                          │   │ 7 d  │12/50│ 85% │       │  │
│  │                          │   └──────┴──────┴──────┘       │  │
│  │                          │                                 │  │
│  │                          │   📅 PRÓXIMA CLASE              │  │
│  │                          │   Álgebra • HOY 14:00 • 60 min │  │
├──┴──────────────────────────┴─────────────────────────────────┴──┤
│ NIVEL 12 ████████████████████░░░░ NIVEL 13  •  850/1000 XP      │ ← BOTTOM (10vh)
│ ¡A 150 XP del siguiente nivel! 🔥                                │   XP BAR ÉPICO
└──────────────────────────────────────────────────────────────────┘
```

### Características Desktop:

- **Header:** 5vh con todo
- **Sidebar Lateral:** 8 botones (left 4, right 4)
- **Main:** 85vh dividido 50/50
  - **Izquierda:** Avatar 3D MÁXIMO (50%, 100% alto)
  - **Derecha:** Info completa (badge XL, XP bar, stats, próxima clase)
- **Bottom:** 10vh XP bar gigante

---

## 💻 CÓDIGO - HubView 50/50 Épico

```tsx
'use client';

import { useState, useEffect } from 'react';
import { motion, AnimatePresence } from 'framer-motion';
import { useDeviceType } from '@/hooks/useDeviceType';
import { AnimatedAvatar3D } from '@/components/3d/AnimatedAvatar3D';
import {
  Home,
  Brain,
  BookOpen,
  Trophy,
  ShoppingBag,
  Menu,
  Coins,
  Flame,
  Star,
  Zap,
  Target,
  Calendar,
  Users,
  BarChart3,
  Bell,
  LogOut,
  Sparkles,
} from 'lucide-react';

export function HubView({ onNavigate, estudiante }: HubViewProps) {
  const { deviceType } = useDeviceType();
  const [showMenuModal, setShowMenuModal] = useState(false);
  const [activeView, setActiveView] = useState('hub');

  // ... resto del state (recursos, racha, etc.)

  return (
    <div className="relative w-full h-screen overflow-hidden bg-gradient-to-br from-slate-900 via-indigo-950 to-slate-900 flex flex-col">
      {/* ========== HEADER MINIMALISTA (5vh) ========== */}
      <header className="relative z-30 h-[5vh] px-4 flex items-center justify-between bg-black/30 backdrop-blur-sm border-b border-white/10">
        {/* Izquierda: Avatar + nombre (SOLO EN TABLET/DESKTOP) */}
        {deviceType !== 'mobile' && (
          <div className="flex items-center gap-2">
            <div className="w-8 h-8 rounded-lg bg-gradient-to-br from-purple-500 to-pink-500 flex items-center justify-center">
              <span className="text-white text-sm font-black">{estudiante.nombre.charAt(0)}</span>
            </div>
            <div className="text-white text-sm font-bold">
              {estudiante.nombre} • Nivel {nivelCalculado}
            </div>
          </div>
        )}

        {/* Centro: Logo (SOLO EN TABLET/DESKTOP) */}
        {deviceType !== 'mobile' && (
          <div className="absolute left-1/2 -translate-x-1/2 text-white text-lg font-black uppercase tracking-wider font-[family-name:var(--font-lilita)]">
            MATEATLETAS
          </div>
        )}

        {/* Derecha: Recursos + Nivel */}
        <div className={`flex items-center gap-2 ${deviceType === 'mobile' ? 'ml-auto' : ''}`}>
          <div className="flex items-center gap-1.5 bg-gradient-to-br from-yellow-400 to-amber-500 rounded-lg px-2 py-1 border border-yellow-600/50">
            <Coins className="w-3.5 h-3.5 text-white" />
            <span className="text-white text-xs font-black">{monedas}</span>
          </div>
          <div className="flex items-center gap-1.5 bg-gradient-to-br from-orange-500 to-red-600 rounded-lg px-2 py-1 border border-red-700/50">
            <Flame className="w-3.5 h-3.5 text-white" />
            <span className="text-white text-xs font-black">{racha_dias}</span>
          </div>
          <div className="flex items-center gap-1 bg-gradient-to-br from-purple-600 to-pink-600 rounded-lg px-2 py-1 border border-white/30">
            <Star className="w-3 h-3 text-white fill-white" />
            <span className="text-white text-xs font-black">{nivelCalculado}</span>
          </div>
        </div>
      </header>

      {/* ========== NAVEGACIÓN LATERAL (SOLO DESKTOP) ========== */}
      {deviceType === 'desktop' && (
        <>
          {/* Sidebar Izquierda */}
          <nav className="fixed left-4 top-1/2 -translate-y-1/2 flex flex-col gap-3 z-50">
            {NAV_LEFT.slice(0, 4).map((item) => (
              <SidebarButton
                key={item.id}
                item={item}
                isActive={activeView === item.id}
                onClick={() => {
                  if (item.overlayId) openOverlay(item.overlayId);
                  else setActiveView(item.id);
                }}
              />
            ))}
          </nav>

          {/* Sidebar Derecha */}
          <nav className="fixed right-4 top-1/2 -translate-y-1/2 flex flex-col gap-3 z-50">
            {NAV_RIGHT.slice(0, 4).map((item) => (
              <SidebarButton
                key={item.id}
                item={item}
                isActive={activeView === item.id}
                onClick={() => {
                  if (item.id === 'cerrar-sesion') setShowLogoutModal(true);
                  else if (item.overlayId) openOverlay(item.overlayId);
                  else setActiveView(item.id);
                }}
              />
            ))}
          </nav>
        </>
      )}

      {/* ========== MAIN CONTENT 50/50 (85vh) ========== */}
      <div className="flex-1 flex items-center px-4 relative z-10">
        <div className="w-full h-full flex gap-4">
          {/* ========== LADO IZQUIERDO - AVATAR GIGANTE (50%) ========== */}
          <div className="w-1/2 h-full relative flex items-center justify-center">
            {/* Resplandor animado gigante */}
            <motion.div
              className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 z-10"
              style={{
                width: deviceType === 'mobile' ? '400px' : '600px',
                height: deviceType === 'mobile' ? '400px' : '600px',
                background:
                  'radial-gradient(circle, rgba(139, 92, 246, 0.5) 0%, rgba(99, 102, 241, 0.4) 25%, rgba(168, 85, 247, 0.3) 50%, transparent 70%)',
                filter: 'blur(50px)',
              }}
              animate={{
                scale: [1, 1.2, 1],
                opacity: [0.6, 0.9, 0.6],
              }}
              transition={{
                duration: 4,
                repeat: Infinity,
                ease: 'easeInOut',
              }}
            />

            {/* Avatar 3D GIGANTE */}
            <div
              className="relative z-20 w-full h-full cursor-pointer"
              onClick={() => {
                const clickUrl =
                  'https://bx0qberriuipqy7z.public.blob.vercel-storage.com/animations/masculine/idle/M_Standing_Idle_Variations_005.glb';
                triggerAnimation(clickUrl, 4000);
              }}
            >
              {isMounted && estudiante.avatar_url && (
                <AnimatedAvatar3D
                  avatarUrl={estudiante.avatar_url}
                  animationUrl={currentAnimation}
                  width="100%"
                  height="100%"
                  cameraPosition={[0, 0.6, 2.2]}
                  cameraFov={50}
                  scale={1.3}
                  position={[-0.3, -0.5, 0]}
                  rotation={[0, 0.3, 0]}
                  enableControls={false}
                />
              )}
            </div>
          </div>

          {/* ========== LADO DERECHO - INFO (50%) ========== */}
          <div className="w-1/2 h-full flex flex-col justify-center gap-4 py-4">
            {/* Badge de nivel */}
            <motion.div
              initial={{ scale: 0 }}
              animate={{ scale: 1 }}
              transition={{ delay: 0.2, type: 'spring' }}
              className="flex justify-center"
            >
              <div
                className={`
                bg-gradient-to-r from-purple-600 to-pink-600
                rounded-2xl border-3 border-white shadow-2xl
                ${deviceType === 'mobile' ? 'px-4 py-2' : 'px-6 py-3'}
              `}
              >
                <div className="text-center">
                  <div className="text-white/80 text-[10px] font-bold uppercase tracking-wider">
                    Nivel
                  </div>
                  <div
                    className={`text-white font-black ${deviceType === 'mobile' ? 'text-3xl' : 'text-5xl'}`}
                  >
                    {nivelCalculado}
                  </div>
                </div>
              </div>
            </motion.div>

            {/* XP Bar (SOLO EN TABLET/DESKTOP) */}
            {deviceType !== 'mobile' && (
              <motion.div
                initial={{ y: 20, opacity: 0 }}
                animate={{ y: 0, opacity: 1 }}
                transition={{ delay: 0.3 }}
                className="bg-black/30 backdrop-blur-sm rounded-xl p-3 border border-white/20"
              >
                <div className="flex items-center justify-between mb-1">
                  <span className="text-white text-xs font-black">NIVEL {nivelCalculado}</span>
                  <span className="text-cyan-300 text-xs font-bold">
                    NIVEL {nivelCalculado + 1}
                  </span>
                </div>
                <div className="h-3 bg-black/40 rounded-full overflow-hidden border border-white/10">
                  <motion.div
                    initial={{ width: 0 }}
                    animate={{ width: `${Math.min(porcentajeProgresoNivel, 100)}%` }}
                    transition={{ delay: 0.5, duration: 1 }}
                    className="h-full bg-gradient-to-r from-yellow-400 via-orange-500 to-red-500 rounded-full"
                  />
                </div>
                <div className="text-white/70 text-[10px] font-bold mt-1">
                  {xpEnNivelActual} / {xpNecesarioParaSiguienteNivel} XP
                </div>
              </motion.div>
            )}

            {/* Stats - 3 columnas (SOLO EN TABLET/DESKTOP) */}
            {deviceType !== 'mobile' && (
              <motion.div
                initial={{ y: 20, opacity: 0 }}
                animate={{ y: 0, opacity: 1 }}
                transition={{ delay: 0.4 }}
                className="grid grid-cols-3 gap-2"
              >
                <StatCardMini
                  icon={<Zap className="w-4 h-4" />}
                  value={`${racha_dias}d`}
                  label="RACHA"
                  gradient="from-orange-500 to-red-600"
                />
                <StatCardMini
                  icon={<Trophy className="w-4 h-4" />}
                  value="12/50"
                  label="LOGROS"
                  gradient="from-yellow-500 to-amber-600"
                />
                <StatCardMini
                  icon={<Target className="w-4 h-4" />}
                  value="85%"
                  label="ÁLGEBRA"
                  gradient="from-purple-500 to-pink-600"
                />
              </motion.div>
            )}

            {/* Stats mini (SOLO MOBILE) */}
            {deviceType === 'mobile' && (
              <div className="flex items-center justify-center gap-3 text-white text-sm font-bold">
                <div className="flex items-center gap-1">
                  <Trophy className="w-4 h-4 text-yellow-400" />
                  <span>12</span>
                </div>
                <div className="flex items-center gap-1">
                  <Zap className="w-4 h-4 text-orange-400" />
                  <span>{racha_dias}d</span>
                </div>
                <div className="flex items-center gap-1">
                  <Target className="w-4 h-4 text-purple-400" />
                  <span>85%</span>
                </div>
              </div>
            )}

            {/* Próxima Clase */}
            {proximaClase && (
              <motion.div
                initial={{ y: 20, opacity: 0 }}
                animate={{ y: 0, opacity: 1 }}
                transition={{ delay: 0.45 }}
                className={`
                  bg-gradient-to-r from-green-500 to-emerald-600
                  rounded-2xl border border-green-400/50
                  ${deviceType === 'mobile' ? 'p-2' : 'p-3'}
                `}
              >
                <div className="flex items-center gap-2">
                  <Calendar className="w-4 h-4 text-white" />
                  <div className="flex-1">
                    <div className="text-white text-xs font-black uppercase">Próxima Clase</div>
                    <div className="text-white/90 text-[10px] font-bold">
                      {esHoy ? 'HOY' : format(fecha, 'dd/MM')} • {format(fecha, 'HH:mm')} hs
                    </div>
                  </div>
                  {esHoy && <div className="w-2 h-2 rounded-full bg-red-500 animate-pulse" />}
                </div>
              </motion.div>
            )}
          </div>
        </div>
      </div>

      {/* ========== BOTTOM BAR ÉPICO (10vh) ========== */}
      <div className="relative z-30 h-[10vh] px-4 flex items-center gap-3 bg-gradient-to-r from-slate-900/98 via-purple-900/98 to-slate-900/98 backdrop-blur-2xl border-t-2 border-white/30">
        {/* MOBILE: XP Bar + Botón Menú */}
        {deviceType === 'mobile' && (
          <>
            <div className="flex-1">
              <div className="flex items-center justify-between mb-1">
                <span className="text-white text-xs font-black">NIVEL {nivelCalculado}</span>
                <span className="text-white/70 text-xs font-bold">
                  {xpEnNivelActual}/{xpNecesarioParaSiguienteNivel} XP
                </span>
              </div>
              <div className="h-3 bg-black/40 rounded-full overflow-hidden border border-white/10">
                <motion.div
                  className="h-full bg-gradient-to-r from-yellow-400 via-orange-500 to-red-500 rounded-full"
                  style={{ width: `${Math.min(porcentajeProgresoNivel, 100)}%` }}
                  animate={{
                    boxShadow: [
                      '0 0 20px rgba(251, 191, 36, 0.6)',
                      '0 0 40px rgba(251, 191, 36, 0.9)',
                      '0 0 20px rgba(251, 191, 36, 0.6)',
                    ],
                  }}
                  transition={{ duration: 2, repeat: Infinity }}
                />
              </div>
            </div>

            <motion.button
              whileTap={{ scale: 0.9 }}
              onClick={() => setShowMenuModal(true)}
              className="flex items-center gap-2 bg-gradient-to-r from-cyan-500 via-purple-600 to-pink-600 rounded-xl px-5 py-2.5 border-2 border-white/50 shadow-[0_0_20px_rgba(139,92,246,0.8)]"
            >
              <Menu className="w-5 h-5 text-white" />
              <span className="text-white text-sm font-black uppercase">MENÚ</span>
            </motion.button>
          </>
        )}

        {/* TABLET: XP Bar + Navegación */}
        {deviceType === 'tablet' && (
          <>
            <div className="flex-[0.6]">
              <div className="flex items-center justify-between mb-1">
                <span className="text-white text-sm font-black">NIVEL {nivelCalculado}</span>
                <span className="text-cyan-300 text-sm font-bold">NIVEL {nivelCalculado + 1}</span>
              </div>
              <div className="h-4 bg-black/40 rounded-full overflow-hidden border border-white/10">
                <motion.div
                  className="h-full bg-gradient-to-r from-yellow-400 via-orange-500 to-red-500 rounded-full relative"
                  style={{ width: `${Math.min(porcentajeProgresoNivel, 100)}%` }}
                >
                  <motion.div
                    className="absolute inset-0 bg-gradient-to-r from-transparent via-white/50 to-transparent"
                    animate={{ x: ['-100%', '200%'] }}
                    transition={{ duration: 2, repeat: Infinity }}
                  />
                </motion.div>
              </div>
              <div className="text-white/70 text-xs font-bold mt-1">
                {xpEnNivelActual} / {xpNecesarioParaSiguienteNivel} XP
              </div>
            </div>

            <div className="flex-[0.4] flex items-center justify-evenly">
              <NavIconButton icon={<Home />} label="HUB" isActive />
              <NavIconButton icon={<Brain />} label="ENTRENAR" badge={3} />
              <NavIconButton icon={<Trophy />} label="LOGROS" />
              <NavIconButton
                icon={<Menu />}
                label="MENÚ"
                isMenu
                onClick={() => setShowMenuModal(true)}
              />
            </div>
          </>
        )}

        {/* DESKTOP: XP Bar gigante */}
        {deviceType === 'desktop' && (
          <div className="flex-1">
            <div className="flex items-center justify-between mb-2">
              <span className="text-white text-lg font-black">NIVEL {nivelCalculado}</span>
              <span className="text-cyan-300 text-base font-bold">
                ¡A {xpNecesarioParaSiguienteNivel - xpEnNivelActual} XP del siguiente nivel! 🔥
              </span>
              <span className="text-white text-lg font-black">NIVEL {nivelCalculado + 1}</span>
            </div>
            <div className="h-6 bg-black/40 rounded-full overflow-hidden border-2 border-white/10 relative">
              <motion.div
                className="h-full bg-gradient-to-r from-yellow-400 via-orange-500 to-red-500 rounded-full relative"
                style={{ width: `${Math.min(porcentajeProgresoNivel, 100)}%` }}
                animate={{
                  boxShadow: [
                    '0 0 30px rgba(251, 191, 36, 0.6), 0 0 60px rgba(251, 191, 36, 0.3)',
                    '0 0 50px rgba(251, 191, 36, 0.9), 0 0 100px rgba(251, 191, 36, 0.5)',
                    '0 0 30px rgba(251, 191, 36, 0.6), 0 0 60px rgba(251, 191, 36, 0.3)',
                  ],
                }}
                transition={{ duration: 2, repeat: Infinity }}
              >
                <motion.div
                  className="absolute inset-0 bg-gradient-to-r from-transparent via-white/60 to-transparent"
                  animate={{ x: ['-100%', '200%'] }}
                  transition={{ duration: 2, repeat: Infinity, repeatDelay: 1 }}
                />
              </motion.div>
              <div className="absolute inset-0 flex items-center justify-center">
                <span className="text-white text-sm font-black drop-shadow-[0_2px_8px_rgba(0,0,0,0.8)]">
                  {xpEnNivelActual} / {xpNecesarioParaSiguienteNivel} XP
                </span>
              </div>
            </div>
          </div>
        )}
      </div>

      {/* ========== MENÚ MODAL ÉPICO ========== */}
      {/* ... código del MenuModal ... */}
    </div>
  );
}

// Componentes auxiliares
function StatCardMini({ icon, value, label, gradient }: any) {
  return (
    <div
      className={`bg-gradient-to-br ${gradient} rounded-xl p-2 border-2 border-white/20 text-center`}
    >
      <div className="text-white mb-1">{icon}</div>
      <div className="text-white text-sm font-black">{value}</div>
      <div className="text-white/80 text-[9px] font-bold uppercase">{label}</div>
    </div>
  );
}

function SidebarButton({ item, isActive, onClick }: any) {
  return (
    <motion.button
      whileHover={{ scale: 1.1 }}
      whileTap={{ scale: 0.9 }}
      onClick={onClick}
      className={`
        w-14 h-14 rounded-2xl
        bg-gradient-to-br ${item.gradient}
        flex items-center justify-center
        border-2 ${isActive ? 'border-white' : 'border-white/20'}
        shadow-lg transition-all
        ${isActive ? 'shadow-[0_0_20px_rgba(139,92,246,0.8)]' : ''}
      `}
    >
      <div className="text-white">{item.icon}</div>
      {item.badge > 0 && (
        <div className="absolute -top-1 -right-1 w-5 h-5 bg-red-500 rounded-full flex items-center justify-center border-2 border-white">
          <span className="text-white text-[9px] font-black">{item.badge}</span>
        </div>
      )}
    </motion.button>
  );
}

function NavIconButton({ icon, label, badge, isActive, isMenu, onClick }: any) {
  return (
    <motion.button
      whileHover={{ scale: 1.1, y: -3 }}
      whileTap={{ scale: 0.9 }}
      onClick={onClick}
      className="flex flex-col items-center gap-1 relative"
    >
      {badge > 0 && (
        <div className="absolute -top-1 -right-1 w-4 h-4 bg-red-500 rounded-full flex items-center justify-center border border-white z-10">
          <span className="text-white text-[8px] font-black">{badge}</span>
        </div>
      )}
      <div
        className={`
        w-12 h-12 rounded-xl flex items-center justify-center border-2 transition-all
        ${
          isActive
            ? 'bg-gradient-to-br from-cyan-500 via-purple-600 to-pink-600 border-white shadow-[0_0_20px_rgba(139,92,246,0.8)]'
            : isMenu
              ? 'bg-gradient-to-br from-orange-500 to-red-600 border-white/30'
              : 'bg-slate-700 border-white/20'
        }
      `}
      >
        <div className="text-white">{icon}</div>
      </div>
      <span
        className={`text-[9px] font-bold uppercase ${isActive ? 'text-white' : 'text-white/70'}`}
      >
        {label}
      </span>
    </motion.button>
  );
}
```

---

## ✅ VENTAJAS DEL LAYOUT 50/50

1. ✅ **Avatar siempre visible y gigante** - Domina su mitad de pantalla
2. ✅ **Sin scroll nunca** - Todo cabe en 100vh (5vh + 85vh + 10vh)
3. ✅ **Navegación épica** - Bottom bar con XP brillante
4. ✅ **Info organizada** - Lado derecho optimizado por dispositivo
5. ✅ **Menú modal tipo Brawl Stars** - Full-screen épico
6. ✅ **Responsive perfecto** - Se adapta mobile/tablet/desktop

---

¿Te gusta esta versión 50/50? ¿Quiero que ahora adapte también la página de crear-avatar para que sea landscape-only y épica?
