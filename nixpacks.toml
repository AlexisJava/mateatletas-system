# Nixpacks configuration for Railway
# Configuración para monorepo de Mateatletas

[phases.setup]
nixPkgs = ["nodejs-20_x", "openssl"]

[phases.install]
cmds = [
    # Instalar todas las dependencias (Railway usa npm ci automáticamente)
    # El comando se ejecuta después de que Railway haga npm ci
    "cd packages/contracts && npm run build"
]

[phases.build]
cmds = [
    # Generar cliente de Prisma
    "cd apps/api && npx prisma generate",

    # Build del API (contracts ya está buildeado y linkeado)
    "cd apps/api && npm run build"
]

[start]
cmd = "cd apps/api && npx prisma migrate deploy && npm run start:prod"

[variables]
NODE_ENV = "production"
