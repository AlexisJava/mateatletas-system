# Entorno (development | production)
# En desarrollo: JWT tokens duran 7 días (evita re-login constante en watch mode)
# En producción: JWT tokens duran 1 hora (mayor seguridad)
NODE_ENV="development"

# Base de datos PostgreSQL
DATABASE_URL="postgresql://user:password@localhost:5432/mateatletas?schema=public"

# JWT (se usara en auth)
JWT_SECRET="tu-secreto-super-seguro-cambialo-en-produccion"
# JWT_EXPIRATION se puede omitir - usa defaults según NODE_ENV
# development: 7d | production: 1h
JWT_EXPIRATION="7d"

# Puertos
PORT=3001
NEXT_PUBLIC_API_URL="http://localhost:3001/api"

# MercadoPago Configuration
# Nota: Usar credenciales de TEST para desarrollo
# Obtener credenciales en: https://www.mercadopago.com.ar/developers/panel/app
MERCADOPAGO_ACCESS_TOKEN="TEST-XXXXXXXX-XXXXXXXX-XXXXXXXX-XXXXXXXX"
MERCADOPAGO_PUBLIC_KEY="TEST-XXXXXXXX-XXXXXXXX-XXXXXXXX-XXXXXXXX"
FRONTEND_URL="http://localhost:3000"
BACKEND_URL="http://localhost:3001"

# Rate Limiting Configuration
# Protege contra brute force, DDoS y abuso de API
RATE_LIMIT_TTL=60000           # 60 segundos (ventana de tiempo)
RATE_LIMIT_MAX=1000            # 1000 requests por minuto (desarrollo)
