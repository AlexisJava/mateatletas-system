# ============================================
# MATEATLETAS API - PRODUCTION CONFIGURATION
# ============================================
# IMPORTANTE: Renombrar a .env.production y configurar valores reales

# Entorno
NODE_ENV="production"

# Base de datos PostgreSQL (PRODUCCIÓN)
# CAMBIAR: Usar credenciales de base de datos de producción
DATABASE_URL="postgresql://user:password@production-db-host:5432/mateatletas?schema=public"

# JWT Secret (CRÍTICO: Cambiar por un valor aleatorio y seguro)
# Generar con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="CAMBIAR-POR-SECRETO-ALEATORIO-DE-128-CARACTERES-MINIMO"
JWT_EXPIRATION="1h"  # Producción: tokens de 1 hora

# Puertos
PORT=3001
NEXT_PUBLIC_API_URL="https://api.mateatletas.com/api"

# MercadoPago Configuration (PRODUCCIÓN)
# CAMBIAR: Usar credenciales REALES de producción
# Obtener en: https://www.mercadopago.com.ar/developers/panel/app
MERCADOPAGO_ACCESS_TOKEN="APP-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"
MERCADOPAGO_PUBLIC_KEY="APP-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"

# URLs de Frontend y Backend (PRODUCCIÓN)
# CRÍTICO: FRONTEND_URL debe estar configurado para CSRF protection
FRONTEND_URL="https://mateatletas.com"
BACKEND_URL="https://api.mateatletas.com"

# Rate Limiting Configuration (PRODUCCIÓN)
# Límites más estrictos para proteger contra abuso
RATE_LIMIT_TTL=60000           # 60 segundos (ventana de tiempo)
RATE_LIMIT_MAX=100             # 100 requests por minuto (producción - más restrictivo)

# ============================================
# CHECKLIST ANTES DE DEPLOY
# ============================================
# [ ] Cambiar DATABASE_URL a base de datos de producción
# [ ] Generar y configurar JWT_SECRET aleatorio (min 128 caracteres)
# [ ] Configurar credenciales REALES de MercadoPago
# [ ] Verificar FRONTEND_URL apunta a dominio de producción
# [ ] Verificar BACKEND_URL apunta a API de producción
# [ ] Ajustar RATE_LIMIT_MAX según tráfico esperado (default: 100)
