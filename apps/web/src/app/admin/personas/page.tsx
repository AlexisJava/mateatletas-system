'use client';

import { PersonasView } from '@/components/admin/views/personas';

/**
 * Admin Personas Page
 *
 * Vista unificada de personas: estudiantes, docentes, tutores, admins.
 */
export default function AdminPersonasPage() {
  return <PersonasView />;
}
