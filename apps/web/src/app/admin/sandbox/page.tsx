'use client';

import { SandboxView } from '@/components/admin/views/sandbox';

/**
 * Admin Sandbox Page
 *
 * Editor visual de contenido educativo basado en JSON.
 * Permite crear lecciones interactivas con componentes del Design System.
 */
export default function SandboxPage() {
  return <SandboxView />;
}
