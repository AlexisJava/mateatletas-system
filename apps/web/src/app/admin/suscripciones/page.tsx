'use client';

import { SuscripcionesView } from '@/components/admin/views/suscripciones';

/**
 * Admin Suscripciones Page
 *
 * Vista de gestión de suscripciones familiares 2026.
 * Permite ver todas las suscripciones, filtrar por estado,
 * y ver el detalle de cada una.
 */
export default function AdminSuscripcionesPage() {
  return <SuscripcionesView />;
}
