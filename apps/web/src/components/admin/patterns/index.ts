// Admin Patterns - Composed components using primitives
export { KPICard } from './KPICard';
export { MiniStat } from './MiniStat';

// Re-export primitives for convenience (GlassCard alias for backward compat)
export { AdminCard as GlassCard } from '../primitives';
